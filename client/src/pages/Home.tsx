import OilWellsMapSplit from "@/components/OilWellsMapSplit";

/**
 * Página principal: Mapa interactivo de pozos de perforación en Asia
 * Split view: información del pozo a la izquierda, Google Maps a la derecha
 */
export default function Home() {
  return <OilWellsMapSplit />;
}
